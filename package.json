{
  "name": "univer-escrow-core",
  "version": "1.0.0",
  "description": "Universal Trust Layer core transaction verification engine.",
  "main": "src/services/MobileGatewayClient.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
