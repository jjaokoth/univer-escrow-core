{
  "name": "universal-trust-layer",
  "private": true,
  "version": "1.0.0",
  "description": "Multi-tenant escrow gateway and transaction router.",
  "main": "dist/server.js",
  "license": "UNLICENSED",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.6.3"
  }
}

